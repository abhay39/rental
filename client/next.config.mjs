/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['picsum.photos','firebasestorage.googleapis.com','res.cloudinary.com']
    }
};

export default nextConfig;
